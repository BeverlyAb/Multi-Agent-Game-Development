# Review: auth-refactor

**Status:** Template — the reviewer agent writes its independent review of
`docs/contract/auth-refactor.md` here.

The reviewer is a separate role (see `docs/reviewer.md`): it inspects the
repository, the contract, and the git diff, classifies findings by severity
(see `docs/finding-severity.md`), assigns each a `P<n>-<NNN>` id, and
reports them without modifying source code.

## Summary

## Findings

| Severity | Finding |
|---|---|
| P0 | |
| P1 | |
| P2 | |
| P3 | |
