"""The workflow's own data-model definitions -- models_verification.py
holds the shared vocabulary (Finding, GuardrailViolation, CallRecord,
ReviewResult) every other file in ../generic/ and ../constraints/
speaks. Named "definitions" to mirror the project's own top-level
definitions/models.py (the actual game's Resident/Building/Task domain)
without being confused for it -- these are two different domains kept
deliberately separate; see ../README.md.
"""
